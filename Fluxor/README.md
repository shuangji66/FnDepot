<p>使用Mihomo内核和MetacubeXD面板的代理软件，方便、快捷地搭建、切换代理节点。</p><p>内置完整config模板和规则集，支持无限添加订阅链接，支持tun透明代理，支持内核、面板在线升级，支持添加自定义面板。</p><p><strong>使用说明：</strong><p></p><p><strong>安装时填写面板密钥，按提示顺序添加订阅名称和订阅链接，选填默认后端地址（仅支持XD面板），选择启动的面板（自定义面板需在“ui8”目录添加面板文件）。启动后直接打开软件或进入“http://fn_ip:port/ui”或反代地址（不建议反代前端/ui入口），后端地址填“http://fn_ip:port”或反代地址，填入自定义的密钥。</strong><p><p><strong>故障处置：</strong><p><p><strong>不能连接后端，先判断浏览器网络环境（局域网、公网、穿透工具组网），添加正确的后端地址。修改过端口/密钥的可清理浏览器缓存再尝试，切换面板需清除缓存和cookies（飞牛app无法彻底缓存，只能重置/重装）</strong><p>